snowflake_demo:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: 'not needed'
      user: 'not needed'
      role: accountadmin
      database: streamlit_apps
      schema: dev
      warehouse: compute_wh
    prod:
      type: snowflake
      account: 'not needed'
      user: 'not needed'
      role: accountadmin
      database: streamlit_apps
      schema: prod
      warehouse: compute_wh
